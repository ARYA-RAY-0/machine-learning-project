# machine-learning-project
I built an automated MLOps pipeline that processes user data from a PostgreSQL database, trains a predictive model with Scikit-Learn, and serves it via an API. The pipeline is fully reproducible using DVC and containerized for production deployment.
